services:
  - type: web
    name: thulasi-wa
    runtime: node
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y chromium && npm install
    startCommand: node server.js
    envVars:
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
